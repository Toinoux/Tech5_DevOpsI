name: CI Frontend
on:
  push:
    branches: [ master ]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  tests:
    name: Test Cypress
    steps:
      - uses: actions/checkout@v2
      - name: setup
        run: |
          yarn
      - name: tests
        run: |
          yarn cypress